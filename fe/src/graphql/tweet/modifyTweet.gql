mutation UNHEART_TWEET($tweet_id: String!) {
  tweet: unheart_tweet(tweet_id: $tweet_id) {
    content
  }
}

mutation HEART_TWEET($tweet_id: String!) {
  tweet: heart_tweet(tweet_id: $tweet_id) {
    content
  }
}
