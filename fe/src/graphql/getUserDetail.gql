query($userId: String) {
  user: user_info(user_id: $userId) {
    _id
    user_id
    name
    comment
    profile_img_url
    background_img_url
    following_id_list
  }
  followerCount: follower_count(user_id: $userId) {
    count
  }
}
